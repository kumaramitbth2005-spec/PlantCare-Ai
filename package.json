{
    "name": "plantcare-ai-platform",
    "version": "1.0.0",
    "description": "PlantCare AI - Complete SaaS Platform",
    "scripts": {
        "install:all": "npm install --prefix backend && npm install --prefix frontend && cd ai_service && pip install -r requirements.txt",
        "dev": "concurrently -n \"front,back,ai\" -c \"cyan,magenta,yellow\" \"npm run dev --prefix frontend -- -p 3000\" \"npm run dev --prefix backend\" \"cd ai_service && python app.py\"",
        "fix-ports": "fix-ports.bat",
        "backend": "npm run dev --prefix backend",
        "frontend": "npm run dev --prefix frontend -- -p 3000",
        "ai": "cd ai_service && python app.py"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}